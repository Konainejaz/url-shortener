<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shorty - Simple URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-slate-50 min-h-screen font-sans text-slate-900">
    <!-- Navbar -->
    <nav class="bg-white/80 backdrop-blur-md border-b border-slate-200 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="flex items-center space-x-2">
                        <div class="bg-blue-600 p-2 rounded-lg">
                            <i class="fas fa-link text-white text-xl"></i>
                        </div>
                        <span class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-indigo-600">Shorty</span>
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <% if (locals.user) { %>
                        <span class="hidden sm:block text-sm font-medium text-slate-500">Hi, <%= user.name %></span>
                        <a href="/dashboard" class="text-sm font-bold text-blue-600 hover:text-blue-700">Dashboard</a>
                        <a href="/logout" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-500 hover:bg-red-600 transition-all">
                            Logout
                        </a>
                    <% } else { %>
                        <a href="/login" class="text-sm font-bold text-slate-600 hover:text-blue-600">Login</a>
                        <a href="/signup" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 shadow-sm transition-all">
                            Get Started
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <header class="relative overflow-hidden pt-16 pb-24 lg:pt-32 lg:pb-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h1 class="text-4xl sm:text-6xl font-extrabold text-slate-900 tracking-tight mb-6">
                Shorten your links, <br>
                <span class="text-blue-600">expand your reach.</span>
            </h1>
            <p class="text-lg sm:text-xl text-slate-600 max-w-2xl mx-auto mb-10">
                Create short, powerful links in seconds. Track your clicks and manage everything from a beautiful dashboard.
            </p>

            <div class="max-w-3xl mx-auto">
                <% if (locals.error) { %>
                    <div class="mb-6 p-4 bg-red-50 border border-red-100 rounded-xl flex items-center gap-3 text-red-700">
                        <i class="fas fa-exclamation-circle text-xl"></i>
                        <p class="font-medium"><%= error %></p>
                    </div>
                <% } %>

                <div class="bg-white p-4 rounded-2xl shadow-xl border border-slate-100">
                    <form action="/url" method="POST" class="space-y-4">
                        <div class="flex flex-col md:flex-row gap-4">
                            <div class="flex-1 min-w-0 px-2">
                                <label for="url" class="block text-xs font-bold text-slate-400 uppercase tracking-wider mb-1 ml-1 text-left">Long URL</label>
                                <input type="url" name="Url" id="url" required
                                    class="block w-full border-0 focus:ring-0 text-slate-900 placeholder-slate-400 text-lg p-0"
                                    placeholder="Paste your long link here...">
                            </div>
                            <div class="md:w-48 px-2 border-t md:border-t-0 md:border-l border-slate-100 pt-4 md:pt-0">
                                <label for="customID" class="block text-xs font-bold text-slate-400 uppercase tracking-wider mb-1 ml-1 text-left">Custom ID (Opt)</label>
                                <input type="text" name="customID" id="customID"
                                    class="block w-full border-0 focus:ring-0 text-slate-900 placeholder-slate-400 text-lg p-0"
                                    placeholder="alias">
                            </div>
                        </div>
                        <button type="submit" 
                            class="w-full inline-flex items-center justify-center px-8 py-4 border border-transparent text-lg font-bold rounded-xl shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none transition-all">
                            <i class="fas fa-magic mr-2"></i> Shorten
                        </button>
                    </form>
                </div>

                <% if (locals.id) { %>
                    <div class="mt-8 p-4 bg-green-50 border border-green-100 rounded-xl flex flex-col sm:flex-row items-center justify-between gap-4">
                        <div class="flex items-center space-x-3">
                            <div class="bg-green-100 p-2 rounded-full">
                                <i class="fas fa-check text-green-600"></i>
                            </div>
                            <div class="text-left">
                                <p class="text-xs font-bold text-green-600 uppercase tracking-wider">Success! Your link is ready</p>
                                <p class="text-slate-900 font-bold font-mono"><%= baseUrl %>/<%= id %></p>
                            </div>
                        </div>
                        <div class="flex items-center space-x-2 w-full sm:w-auto">
                            <button onclick="copyToClipboard('http://localhost:8000/<%= id %>')" 
                                class="flex-1 sm:flex-none px-4 py-2 bg-white border border-slate-200 text-slate-700 rounded-lg text-sm font-bold hover:bg-slate-50 transition-all">
                                Copy
                            </button>
                            <a href="http://localhost:8000/<%= id %>" target="_blank"
                                class="flex-1 sm:flex-none px-4 py-2 bg-blue-600 text-white rounded-lg text-sm font-bold hover:bg-blue-700 transition-all">
                                Visit
                            </a>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
        
        <!-- Background Decoration -->
        <div class="absolute top-0 left-1/2 -translate-x-1/2 -z-10 w-full max-w-6xl opacity-20 blur-3xl pointer-events-none">
            <div class="aspect-[1100/600] bg-gradient-to-tr from-blue-400 to-indigo-600 rounded-[40%] rotate-12"></div>
        </div>
    </header>

    <!-- Features Section -->
    <section class="py-24 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16">
                <h2 class="text-3xl font-bold text-slate-900">Why choose Shorty?</h2>
                <p class="mt-4 text-slate-600">Everything you need to manage your links effectively.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
                <div class="text-center">
                    <div class="w-16 h-16 bg-blue-100 text-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-6">
                        <i class="fas fa-bolt text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">Lightning Fast</h3>
                    <p class="text-slate-600">Shorten links in a blink of an eye. No delays, no waiting.</p>
                </div>
                <div class="text-center">
                    <div class="w-16 h-16 bg-indigo-100 text-indigo-600 rounded-2xl flex items-center justify-center mx-auto mb-6">
                        <i class="fas fa-chart-pie text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">Detailed Analytics</h3>
                    <p class="text-slate-600">Track every click. Know where your audience comes from.</p>
                </div>
                <div class="text-center">
                    <div class="w-16 h-16 bg-purple-100 text-purple-600 rounded-2xl flex items-center justify-center mx-auto mb-6">
                        <i class="fas fa-shield-alt text-2xl"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">Secure & Private</h3>
                    <p class="text-slate-600">Your data is safe with us. We prioritize your privacy.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-slate-50 border-t border-slate-200 py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <div class="flex items-center justify-center space-x-2 mb-6">
                <div class="bg-blue-600 p-1.5 rounded-lg">
                    <i class="fas fa-link text-white text-sm"></i>
                </div>
                <span class="text-lg font-bold text-slate-900">Shorty</span>
            </div>
            <p class="text-slate-500 text-sm">© 2026 Shorty URL Shortener. Built with ❤️ for the web.</p>
        </div>
    </footer>

    <script>
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                alert('Link copied to clipboard!');
            }).catch(err => {
                console.error('Failed to copy: ', err);
            });
        }
    </script>
</body>
</html>
